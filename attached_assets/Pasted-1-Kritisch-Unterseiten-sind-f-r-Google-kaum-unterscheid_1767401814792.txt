1) Kritisch: Unterseiten sind für Google kaum unterscheidbar

Bei mehreren URLs sieht der sichtbare Content sehr ähnlich/gleich aus (z. B. /about, /faq, /ai-agencija-hrvatska zeigen denselben großen Hero-Block „AI agenti i automatizacija…“). Das wirkt für Google wie Duplicate/Thin Content → Unterseiten ranken schlecht. 
KSHWmont
+3
KSHWmont
+3
KSHWmont
+3

✅ Fix:

Pro URL eigene H1 + eigener Text (mind. 200–400 Wörter) + interne Links.

Wenn SPA: Prerender/SSR (sonst kommen Titles/Meta/Content oft nicht pro Route sauber an).

2) Kritisch: Title ist auf allen Seiten gleich (du hast es gemessen)

Das ist ein echter Ranking-Killer, weil Google Seiten nicht auseinanderhalten kann.

✅ Fix:

Pro Seite eindeutiger <title> + meta description + canonical.

Beispiel-Regel:
/leistungen/webdesign → „Webdesign Zagreb | Responsive Websites – KSHWmont“
/faq → „FAQ SEO & AI – KSHWmont“
/standorte/split → „SEO & AI Split – KSHWmont“

3) Sprachen/Internationalisierung: wirkt inkonsistent

Die Seite mischt HR/DE (Language Switch “HR/DE”, gleichzeitig deutsche und kroatische Seiten/Slugs). Ohne hreflang und klare Sprachpfade kann das zu Kannibalisierung führen. 
KSHWmont
+2
KSHWmont
+2

✅ Fix:

Pro Seite korrektes <html lang="de"> bzw. lang="hr".

hreflang-Mapping (de ↔ hr) sauber ausspielen (am besten feste Pfade z. B. /de/... und /hr/...).

4) Sitemap/robots: Tool konnte sie nicht öffnen → bitte Status prüfen

Mein Web-Viewer bekam bei /sitemap.xml, /ai.sitemap.xml, /robots.txt “Internal Error”. Das heißt nicht sicher, dass sie kaputt sind – aber wir müssen die HTTP-Status + Content-Type verifizieren. 
Ungültige URL
+2
+2

✅ Bitte auf dem VPS ausführen:

curl -I https://kshwmont.com/sitemap.xml
curl -I https://kshwmont.com/ai.sitemap.xml
curl -I https://kshwmont.com/robots.txt
curl -s https://kshwmont.com/robots.txt


Soll: 200 und content-type: application/xml bzw. text/plain.

5) Struktur/UX-Signale

Homepage ist klar positioniert („SEO-Optimierung, GEO & KI-Chatbots Zagreb …“), das ist gut – aber es muss sich pro Leistungs-/Standortseite in eigenem Content fortsetzen. 
KSHWmont
+1

✅ Fix:

Interne Verlinkung: jede Leistungsseite → 3–5 relevante Services + Standort + FAQ-Teaser.

Schema.org: Organization/ProfessionalService + Service pro Leistung + FAQPage nur auf /faq.

Was ich als nächstes machen würde (Priorität)

Titles/Descriptions pro Route technisch fixen (dein #1 Hebel).

Prerender/SSR, damit Google pro URL wirklich eigenes HTML bekommt.

Content pro Seite unique + interne Linkstruktur + Schema.

Wenn du mir sagst, ob das Projekt Vite/React (dist) oder Next (.next) ist, gebe ich dir den exakten Code-Fix für unique Title/Meta pro Route + Prerender/SSR passend zu deinem Setup.